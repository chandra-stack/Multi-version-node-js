name: failure test

on:
  push:

jobs:
  Test:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v2
      name: Maven command
    - run: mvn deploy
      if: ${{ failure() || success() }}
      name: additional commands
    - run: echo "chandru"
      
