name: failure test

on:
  push:

jobs:
  Test:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v2
      name: Maven command
    - run: mvn deploy
      name: additional commands
      if: ${{ failure() || success() }}
    - run: echo "chandru"
      
